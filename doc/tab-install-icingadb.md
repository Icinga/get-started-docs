=== "Ubuntu"

    ```bash
    apt install icingadb
    ```

=== "Debian"

    ```bash
    apt install icingadb
    ```

=== "SLES"

    ```bash
    zypper install icingadb
    ```

=== "RHEL"

    === "RHEL 8 or Later"

        ```bash
        dnf install icingadb
        ```

    === "RHEL 7"

        ```bash
        yum install icingadb
        ```
        